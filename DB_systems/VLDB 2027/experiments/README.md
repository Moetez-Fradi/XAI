# `experiments/` — local run archives (VLDB 2027)

Timestamped CSV / plot outputs. **Gitignored** (regenerable). Canonical figures
shipped with the PDF live in [`../paper/figures/`](../paper/figures/).

```
<YYYY-MM-DD_HH-MM-SS>__<step_id>__<metric>/
├── manifest.json
├── results/*.csv
└── plots/*.png|pdf
```

See [`../README.md`](../README.md) for the paper → `step_id` map.
