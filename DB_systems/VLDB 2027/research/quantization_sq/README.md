# Fig 21 — scalar quantization characterization

Builds an FP collection and an SQ-int8 collection. Masked focus on SQ is
expected to 400 (fail-fast).

```bash
python run_sq_char.py --mode simulated --queries 8 --trials 1
```
